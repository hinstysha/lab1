#!/bin/sh
# Хук для проверки кода линтером flake8 перед коммитом
echo "Запуск flake8..."
flake8 .
if [ $? -ne 0 ]; then
  echo "❌ Найдены ошибки flake8. Исправьте их перед коммитом."
  exit 1
fi
echo "✅ Проверка flake8 пройдена!"
